# 微信公众号采集问题诊断报告

**诊断时间**: 2026-02-07 23:40
**问题**: 刘小排r等公众号显示0篇文章，但实际23小时前刚推送过

---

## 🔍 问题现象

### 用户反馈
> "刘小排r 23小时前刚推送过，但采集显示0篇文章"

### 数据验证
| 公众号 | 数据库文章数 | 采集结果 |
|--------|------------|---------|
| 刘小排r | 2篇（最新2月4日） | 0篇 ❌ |
| 红色星际 | 0篇 | 0篇 ❌ |
| 极客公园 | 24篇 | 1篇 |
| 深蓝AI | 14篇 | 1篇 |
| 其他批次B公众号 | 有数据 | 0篇 |

---

## 🔴 根本原因

### Wewe-RSS账号失效

**错误日志**（持续报错）:
```
ERROR: 暂无可用读书账号!
时间: 22:10 - 22:13（每30秒一次）
来源: Wewe-RSS FeedsService
```

**影响**:
- ❌ 无法同步最新的公众号文章
- ❌ 只能读取数据库中的旧数据
- ❌ 刘小排r23小时前的文章无法获取
- ❌ 大部分公众号显示0篇文章

---

## 📊 详细分析

### Wewe-RSS服务状态
```
容器状态: Up 15 hours (healthy)
健康检查: 通过
端口: 4000
账号状态: ❌ 失效（暂无可用读书账号）
```

### 数据库状态
```
刘小排r文章:
  - 总数: 2篇
  - 最新: 2026-02-04 10:22:11（UTC）
  - 距今: 约3天（而不是23小时）

结论: 数据库中没有23小时前的文章
```

### 采集逻辑分析

**今日采集**（批次B，周五）:
```
采集13个公众号
结果: 2篇文章（都是旧文章）
原因: Wewe-RSS无法获取新数据
```

**分批策略**:
- 批次A: 周一、三、五、日（14个公众号）
- 批次B: 周二、四、六（13个公众号）
- **今天是周五，应该采集批次A，但采集了批次B**

**⚠️ 发现另一个问题**:
- 今天是周五（2026-02-07）
- 按配置应该采集批次A
- 但实际采集的是批次B
- **可能是分批逻辑错误或时区问题**

---

## 🔧 解决方案

### 方案1：重新登录微信读书（立即）⭐

**步骤**:

1. **访问Wewe-RSS管理页面**
   ```
   http://localhost:4000
   或
   http://127.0.0.1:4000
   ```

2. **重新登录账号**
   - 点击右上角"账号管理"或"登录"
   - 使用微信扫描二维码
   - 确认登录成功

3. **手动触发同步**
   - 点击"立即同步"按钮
   - 等待同步完成（约2-5分钟）

4. **验证同步结果**
   ```bash
   # 检查日志，应该不再有"暂无可用账号"错误
   docker logs wewe-rss --tail 50

   # 查看同步的文章数量
   docker exec wewe-rss ls -lh /app/data/wewe-rss.db
   ```

5. **重新触发采集**
   ```bash
   docker exec wechat-service python main.py run
   ```

### 方案2：检查分批逻辑（修复时区问题）

**问题**:
- 今天是周五，应该采集批次A（14个公众号）
- 但实际采集了批次B（13个公众号）

**可能原因**:
- 时区配置错误
- 分批逻辑判断错误
- UTC vs 北京时间混淆

**检查配置**:
```bash
# 查看微信服务配置
docker exec wechat-service env | grep TZ

# 查看分批配置
docker exec wechat-service cat /app/config.yaml | grep -A 5 "batch_mode"
```

---

## 📋 验证清单

### 登录后验证

- [ ] Wewe-RSS日志不再显示"暂无可用账号"
- [ ] 刘小排r有新文章（23小时前的）
- [ ] 采集时显示新增文章数 > 0
- [ ] 各公众号文章数量更新

### 测试命令

```bash
# 查看Wewe-RSS日志
docker logs wewe-rss --tail 100 | grep -E "ERROR|sync|成功"

# 查看刘小排r最新文章
docker exec wechat-service python -c "
import sqlite3
conn = sqlite3.connect('/app/data/wechat.db')
cursor = conn.cursor()
cursor.execute('SELECT title, published_at FROM articles WHERE feed_name=\"刘小排r\" ORDER BY published_at DESC LIMIT 5')
for row in cursor.fetchall():
    print(f'{row[0]} - {row[1]}')
conn.close()
"

# 重新触发采集
docker exec wechat-service python main.py run
```

---

## 🎯 预期结果

### 重新登录后

```
采集结果应该是:
- 刘小排r: 应该有23小时前的文章
- 红色星际: 可能有新文章
- 批次B各公众号: 有最新文章
- 新增文章数: > 0
```

### 数据库状态

```
刘小排r文章应该包含:
- 最新文章: 23小时前（2026-02-06左右）
- 文章数: > 2篇
```

---

## ⚠️ 注意事项

### 1. 账号有效期

微信读书账号有时效性，可能需要定期重新登录。

建议:
- 每周检查一次账号状态
- 设置监控告警（检测到"暂无可用账号"时发送通知）

### 2. 分批逻辑问题

今天（周五）应该采集批次A，但采集了批次B，说明分批逻辑有问题。

建议:
- 检查时区配置
- 检查分批判断逻辑
- 添加日志记录分批选择过程

### 3. 采集频率

当前配置: 每2小时自动同步（Wewe-RSS）
- CRON_EXPRESSION=0 */2 * * *

但账号失效后，同步一直失败。

建议:
- 添加账号失效检测
- 失败时发送告警通知

---

## 📞 下一步行动

1. **立即**: 访问 http://localhost:4000 重新登录
2. **短期**: 修复分批逻辑的时区问题
3. **长期**: 添加账号失效监控和告警

---

## 📊 相关日志

### Wewe-RSS错误日志（最近）
```
2026-02-07 22:10:30 ERROR [FeedsService] handleUpdateFeedsCron error
2026-02-07 22:10:30 ERROR [FeedsService] Error: 暂无可用读书账号!
2026-02-07 22:11:00 ERROR [FeedsService] handleUpdateFeedsCron error
2026-02-07 22:11:00 ERROR [FeedsService] Error: 暂无可用读书账号!
... (持续重复)
```

### 微信服务采集日志（今天）
```
2026-02-07 23:29:28 INFO 开始采集 13 个公众号
2026-02-07 23:29:57 INFO 采集完成: 总计 2 篇，新增 0 篇
```

---

**报告生成时间**: 2026-02-07 23:40
**执行人**: Claude Code AI Assistant
**版本**: v1.0
